<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BrazmasCheckIn" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
       <item name="colorPrimary">@color/primaryColor</item>
    </style>

    <style name="Theme.BrazmasCheckIn" parent="Base.Theme.BrazmasCheckIn" />
    <style name = "App.Custom.Indicator" parent = "Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name = "android:color">@color/navBarGrey</item>

    </style>

    <style name="MyDatePickerStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/primaryColor</item>

        <item name="android:textColorPrimary">@color/black</item> <!-- Change the textColorPrimary to your desired text color -->
        <item name="android:calendarTextColor">@color/black</item>
        <item name="android:yearListSelectorColor">@color/black</item> <!-- Change the yearListSelectorColor to your desired year selector color -->

    </style>

</resources>